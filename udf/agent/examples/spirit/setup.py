from setuptools import setup, find_packages

setup(
        name='dial',
        version='1.0.0',
        description='packages used by the dial project',
        author='tct',
        classifiers=[
            'Programming Language :: Python :: 2.7'
            ],
        packages=find_packages(),
        )
